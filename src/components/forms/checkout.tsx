"use client";
import Link from "next/link";
import { useState } from "react";
import { useCart } from "@/components/cart/cart-provider";
import { dishes, formatPrice } from "@/data/menu";
import { validPhone } from "@/lib/validation";
type Confirmation={name:string;total:number;reference:string};
export function Checkout(){const{lines,total,dispatch,setOpen}=useCart();const[error,setError]=useState("");const[confirmation,setConfirmation]=useState<Confirmation|null>(null);if(confirmation)return <div className="checkout-success"><p className="eyebrow">Demo order complete</p><h1 className="section-title">Thank you, {confirmation.name}.</h1><p>Your demo order total is {formatPrice(confirmation.total)}.</p><p>Reference: {confirmation.reference}</p><p className="muted">This order was not sent to a restaurant. No payment was taken.</p><Link href="/" className="button">Back to Home</Link></div>;if(!lines.length)return <div className="empty-state center"><h1 className="section-title">Your cart is empty</h1><p className="section-subtitle">Choose a few favourites before checking out.</p><Link href="/#order" className="button">Explore dishes</Link></div>;return <><p className="eyebrow">One more step</p><h1 className="section-title">Checkout</h1><div className="checkout-grid"><form className="checkout-form" onSubmit={event=>{event.preventDefault();const data=new FormData(event.currentTarget);const name=String(data.get("name")).trim();if(name.length<2){setError("Please enter your name.");return;}if(!validPhone(String(data.get("phone")))){setError("Please enter a valid phone number.");return;}setConfirmation({name,total,reference:`DEMO-${crypto.randomUUID().slice(0,8).toUpperCase()}`});dispatch({type:"clear"});}}><h2>Pickup details</h2><label>Full name<input name="name" autoComplete="name" required minLength={2} maxLength={80}/></label><label>Phone number<input name="phone" type="tel" autoComplete="tel" required maxLength={20}/></label><label>Order notes <span className="muted">(optional)</span><textarea name="notes" rows={3} maxLength={300} placeholder="Any requests for your order?"/></label><div className="demo-callout">This is a demo checkout. No real order will be placed and no payment will be collected.</div>{error&&<p className="form-error" role="alert">{error}</p>}<button className="button full" type="submit">Place Demo Order · {formatPrice(total)}</button></form><aside className="order-summary"><h2>Your order</h2>{lines.map(line=>{const dish=dishes.find(d=>d.slug===line.slug)!;return <div className="summary-line" key={line.slug}><span>{line.quantity} × {dish.name}</span><strong>{formatPrice(dish.price*line.quantity)}</strong></div>})}<div className="summary-line"><span>Pickup</span><span>Free</span></div><div className="cart-total"><span>Total</span><strong>{formatPrice(total)}</strong></div><button className="button outline full" onClick={()=>setOpen(true)}>Edit Cart</button></aside></div></>}
