"use client";
import Image from "next/image";
import Link from "next/link";
import { useState } from "react";
import { categories, dishes, formatPrice, Category } from "@/data/menu";
import { useCart } from "@/components/cart/cart-provider";
export function ProductGrid({initialCategory="All",order=true}:{initialCategory?:Category|"All";order?:boolean}){const[category,setCategory]=useState<Category|"All">(initialCategory);const[notice,setNotice]=useState("");const{dispatch}=useCart();const filtered=category==="All"?dishes:dishes.filter(d=>d.category===category);return <><div className="category-filters" aria-label="Filter dishes">{(["All",...categories] as const).map(item=><button key={item} aria-pressed={category===item} className={category===item?"filter active":"filter"} onClick={()=>setCategory(item)}>{item}</button>)}</div><p className="cart-notice" role="status">{notice || "Freshly prepared, made for you."}</p><div className="product-grid">{filtered.map(dish=><article className="product-card" key={dish.slug}><Link href={`/menu/${dish.slug}/`} className="product-photo" aria-label={`View ${dish.name} details`}><Image src={dish.image} alt={dish.name} fill sizes="(max-width: 700px) 100vw, 33vw"/></Link><div className="product-copy"><Link href={`/menu/${dish.slug}/`}><h3>{dish.name}</h3></Link><p>{dish.description}</p><div className="product-bottom"><strong>{formatPrice(dish.price)}</strong><span>{dish.portion}</span></div>{order?<button className="button full" onClick={()=>{dispatch({type:"add",slug:dish.slug});setNotice(`${dish.name} added to your cart.`);}}>Add to Cart <span aria-hidden>+</span></button>:<Link className="button outline full" href={`/menu/${dish.slug}/`}>View Details</Link>}</div></article>)}</div></>}
